import datetime;
import decimal;

class Estados:
    id: int = 0;
    nombre: str = None;

"""
    int (int-long)
    float, decimal, complex
    str
    datetime (datetime.datetime.now())
    bool (True, False)
"""

estado = Estados();
print("Hola mundo!");